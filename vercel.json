{
  "version": 2,
  "builds": [
    {
      "src": "pom.xml",
      "use": "@vercel/java",
      "config": {
        "buildCommand": "mvn clean install",
        "outputDirectory": "target"
      }
    }
  ]
}
